spring:
  application:
    name: devpilot-ai

  datasource:
    url: jdbc:postgresql://localhost:5432/devpilot
    username: postgres
    password: vjcool

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        formal-sql: true

server:
  port: 8080